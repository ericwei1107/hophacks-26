# Space-weather intake

Place source files here before loading them. Keep one source per file and retain
the source URL, retrieval timestamp, and measurement timestamp with every row.

`observations.csv` defines the normalized import shape. Do not add production
data to this template.
