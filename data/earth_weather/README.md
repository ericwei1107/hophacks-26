# Earth-weather intake

Place source files here before loading them. Use UTC timestamps and record the
geographic coordinates and source URL for every observation.

`observations.csv` defines the normalized import shape. Do not add production
data to this template.
