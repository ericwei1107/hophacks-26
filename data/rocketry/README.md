# Rocketry intake

Store motor, vehicle, launch, and regulatory datasets here. Keep source URLs
and dataset version or retrieval date in every imported file. The existing
motor catalog files are retained as the first rocketry source.

`motors.csv` is the normalized motor import template; use `source_url` as the
stable source key when deduplicating records.
