# Preset CAD models

These display-scale models are generated from `web/src/data/rocket-presets.json`
with Build123d. They depict the app's fictional, two-stage vehicles at 1:100;
they are not manufacturing or flight-qualified designs.

Regenerate all STEP and STL files with:

```bash
python scripts/generate_rocket_models.py
```
